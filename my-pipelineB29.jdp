pipeline {
    agent any

    environment {
        JAVA_HOME = "/usr/lib/jvm/java-17-openjdk-amd64"
        PATH = "${JAVA_HOME}/bin:${env.PATH}"
    }

    stages {

        stage('Clean Workspace') {
            steps {
                echo 'Cleaning workspace...'
                deleteDir()
            }
        }

        stage('Checkout') {
            steps {
                echo 'Checking out code from GitHub...'
                git branch: 'master', url: 'https://github.com/rushalitambuskar178-bot/my-new-folder.git'
            }
        }

        stage('Verify Structure') {
            steps {
                sh 'ls -R'
            }
        }

        stage('Build') {
            steps {
                dir('backend') {
                    echo 'Building backend...'
                    sh 'java -version'
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Test') {
            steps {
                sh '''mvn sonar:sonar \\
                -Dsonar.projectKey=my-project-b29 \\
                -Dsonar.projectName=\'my-project-b29\' \\
                -Dsonar.host.url=http://13.61.91.228:9000 \\
                -Dsonar.token=sqp_35b7ac962aedc83918807613547407bccbfd5d0c'''
                echo 'Running tests...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application...'
            }
        }
    }

    post {
        success {
            echo 'Pipeline executed successfully 🎉'
        }
        failure {
            echo 'Pipeline failed ❌ check logs'
        }
    }
}
}
}
